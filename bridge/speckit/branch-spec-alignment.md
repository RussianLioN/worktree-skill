# Branch-Spec Alignment

- Prefer numeric feature branches such as `NNN-<slug>` for Speckit-driven work.
- Keep one spec package aligned to one feature line.
- Do not let worktree helpers silently fork branch naming away from the active Speckit package.
