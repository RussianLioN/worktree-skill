# Handoff Templates

This directory is reserved for portable handoff templates that:

- stop after worktree setup
- preserve branch and path context
- keep downstream task work deferred to the target worktree session

Host projects may add richer overlays, but the portable templates here should remain project-agnostic.
