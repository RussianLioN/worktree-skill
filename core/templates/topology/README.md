# Topology Templates

This directory holds portable defaults for topology registry docs and intent sidecars.

The host project may override final target paths through environment variables or install-time configuration.
