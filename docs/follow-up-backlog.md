# Follow-Up Backlog

- Add richer optional issue-tracker adapters beyond the current baseline
- Add adapter-specific validation tests for more IDE runtimes
- Add packaged release artifacts for non-copy install flows
- Add more host overlay templates for projects with existing worktree helpers
- Reduce remaining optional Beads-oriented assumptions inside the extracted core scripts
