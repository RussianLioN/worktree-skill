# Existing Project Example

1. Audit the host project for `.claude/`, `scripts/`, and worktree-related files.
2. Install `worktree-skill` into a staging branch.
3. Resolve path collisions before merge.
4. Keep host-only governance files in the host project.
5. Run `install/verify.sh`.
