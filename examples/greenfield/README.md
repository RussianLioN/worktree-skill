# Greenfield Example

1. Start with a new git repository.
2. Copy `core/`.
3. Add one adapter.
4. Optionally add the Speckit bridge.
5. Run `install/verify.sh`.
6. Invoke the worktree flow in the selected IDE.
